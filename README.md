# Emotion-sentiment-analysis
Multi-label emotion classification on SemEval-2018 Task 1 (11 emotions) using fine-tuned DistilBERT — improves over the official BoW baseline with contextual embeddings, AdamW, class-weighted BCE loss, and per-label threshold tuning.
